import { IsInCatalogPipe } from './is-in-catalog.pipe';

describe('IsInCatalogPipe', () => {
  it('create an instance', () => {
    const pipe = new IsInCatalogPipe();
    expect(pipe).toBeTruthy();
  });
});
